{% extends "layout.njk" %}

{% block body %}
    <section class="container blog-post">
        <header>
            {% if header_img and header_img_alt %}
                <picture>
                    <img src="{{header_img}}" alt="{{header_img_alt}}"/>
                </picture>
            {% endif %}
            <h1>{{title}}</h1>
            <p>{{ author or metadata.author.name }}<br/>{{ date | readableDate }}</p>
        </header>
        <main class="blog-content">
            {{content | safe}}
        </main>
    </section>
{% endblock %}